version: 2

models:
  - name: game_players
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_start
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_rounds
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_status
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_power_up
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_score
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: game_end
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: games
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: sevenrooms_boxes
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
  - name: google_responses
    config:
      materialized: table
      database: "{{ env_var('DBT_DWDB_NAME') }}"
      schema: intermediate
