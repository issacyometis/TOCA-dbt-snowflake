version: 2

models:
    - name: stg_epos__sales_detail
      config:
        database: "base"
        schema: epos
    - name: stg_epos__sales_header
      config:
        database: "base"
        schema: epos
    - name: stg_epos__sales_discount
      config:
        database: "base"
        schema: epos
    - name: stg_epos__sales_payment
      config:
        database: "base"
        schema: epos
    - name: stg_epos__tender_type
      config:
        database: "base"
        schema: epos